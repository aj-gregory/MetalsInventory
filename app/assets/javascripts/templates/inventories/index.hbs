<h1>My Inventories</h1>

<ul>
  {{#each inventory in controller}}
    <li>
      {{#link-to 'inventory' inventory}}{{inventory.name}}{{/link-to}}
    </li>
  {{/each}}
</ul>

{{input type="text" id="new-inventory" placeholder="Add an inventory"
  value=newName action="createInventory"}}

