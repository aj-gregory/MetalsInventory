
<% if ! user_signed_in? %>
  <%= link_to('Sign In', new_user_session_url, :class => "sign_in") %>
<% else %>
  <%= link_to('Sign Out', destroy_user_session_url, :method => :delete) %>
  <br>
<% end %>


